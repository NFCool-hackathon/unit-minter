import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'nfcool-unit-minter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
